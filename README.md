# JSON to CSV Desktop Program

This project reads JSON files, processes the data, and writes it to CSV files.

## Requirements
- Java 17+
- Maven

## Run
```bash
mvn clean package
java -jar target/json-csv-app.jar
